# L2.2 长任务验收记录

**2026-09-19 当前验收状态：** 冻结BC在隔离qwen3-max生成／复核下完成L2.2既定Phase验收：20/20类型结构、16/16内容、4/4拒答；391项Python／独立PostgreSQL测试、20份产物重启与幂等恢复、浏览器、取消及删除检查通过。范围为两门已见课程、四段视频上的10道开发与10道新目标，AI来源核查，不代表未见媒体泛化。候选未发布，线上C及个人模型配置不变；L3未启动。 见 [最终报告](../eval/l22-completion/FINAL_BC.md)。

以下保留按日期与候选记录的历史过程；旧轮次失败不被覆盖。

2026-09-19 启动。目标是完成课程草稿复核、可靠修订与拒答的质量验收；不是仅完成一轮提示调整。启动时状态：**进行中，未通过，不部署**；现已按原门槛完成，未部署。

## 不变的完成条件

沿用 [Study v1 评测约定](../eval/study-v1/README.md)和 [L2.2 门槛](L2_QUALITY_EVALUATION.md)：

- 全部 20 个任务入账，开发/新保留任务分别报告，不排除失败或超时。
- 至少 90% 正确类型与结构；全部成功练习的引用正文、来源和时间戳有效。
- 16 个支持题至少 15 个解释、参考答案、逐题引用和题型区别同时合格；无严重无据断言。
- 4 个课程外目标全部明确证据不足。
- 至少 18/20 在原有 90 秒内得到正确类型产物；报告包含失败的 P50/P95。
- 冻结候选、模型、数据和预算，使用新的保留任务；与同配置、同课程和任务的旧实现比较，不重计历史失败。
- Python 与独立 PostgreSQL 机制验证通过；真实模型任务质量独立验收。补齐实际 Evidence/鉴权入口和产物恢复检查，固定证据小样本不能替代完整验收。

线上 C、个人模型连接与用途配置保持不变。候选只在隔离评测环境运行，未经发布授权不部署。L3 不属于本任务。

## 工作顺序

1. 消除标准答案与自由评分条款的双重来源：模型提交任务相关答案要点，Python 从同一要点生成参考答案与评分依据。旧自由文本草稿不被静默“修正后算对”。
2. 将新契约的课程复核收敛为每字段一个结论，避免课程求解、评分规则与额外问题报告互相污染；保留有来源的修订观察、失败记录、持久预算与恢复。
3. 先在明确已见的开发目标上验证完整生成—复核—修订/停止。达到进入验收的条件后冻结源码，再写新保留任务。
4. 完整运行同配置基线和候选；对所有产物逐题做来源复核，按原门槛计算结果。失败留存；若候选要继续修改，已消费保留题降为开发材料，并另写新保留题。
5. 同步实现、架构、部署说明与最终验收报告，仅在全部必需条件达到时标记长任务完成。

历史 M 候选及其失败保持不变，见 [M 报告](../eval/reviewer-probe/COURSE_ANSWER_FIRST.md)。本任务不通过改标签、降低门槛或增加 deadline 宣称完成。

## 执行进度

已实现答案/评分依据同源、完整引用回填、独立求解及其有版本范围的复用、显式预算观察、有限且留痕的参数兼容处理。隔离 Java/Python/数据库环境已完成两段真实视频上传、鉴权与 Evidence 索引，开始完整入口开发回归。最新结果和仍未解决的问题见[长任务开发记录](../eval/l22-completion/README.md)。

历史开发失败全部保留。AT 已完成冻结源码、冻结后新保留题、同配置旧 C 配对及正式二十题验收，但**未通过**：AT 正确类型16/20、内容9/16、拒答4/4；旧 C 为19/20、3/16、4/4。AT此前单独开发回归8/8未在正式运行复现，不能替代正式结果。[完整配对报告](../eval/l22-completion/FINAL_AT.md)。

AT全套323项 Python/独立 PostgreSQL机制测试通过、无跳过，真实浏览器、16份成功产物的重启恢复、START幂等重放与取消验证通过；机制通过不证明教学质量。最终删除验收未执行，隔离课程和失败状态仍保留供后续开发，清理前将私有备份完整历史。

当前继续修复：拒绝意见同时含候选字段、两条课程来源及目标原文时的内部容量冲突；已核算答案的语义复核边界；输出截断的预算内停止/纠正；排序中间状态的计算正确性。AT保留题已经消费，后继候选必须冻结后另写新保留题。用户已授权隔离qwen3-max生成，线上C与个人配置不变。**Phase未完成，不进入L3，不部署。**

2026-09-19 AU 后续：20 项已见开发回归为 18/20 类型结构、11/16 完整语义、4/4 拒答（71 次真实调用），仍未通过。343 项机制测试通过不改变 Phase 未完成状态。详见 [AU 报告](../eval/l22-completion/au-max-full-development-report.json)。继续 AV 修复，尚未创建新保留集。

后续 AV/AW 二十题已见回归分别为12/16与9/16完整语义，均未达标；两版类型结构18/20、拒答4/4。AX 通过350项机制测试后继续开发回归，尚无新的保留集。线上C与原模型配置保持不变。

AX已见回归进一步达到19/20类型结构、14/16完整语义、4/4拒答，仍未达标。AY修复新增课程结果引用的短ID转换，350项机制测试通过，正在开发回归；正式门槛仍以新的完整配对保留集为准。

AY已见回归为20/20结构、14/16语义、4/4拒答；AZ为19/20、13/16、4/4，成本应用已修复但区间概念与拒绝协议仍失败。BA在367项机制测试通过后冻结并继续开发回归，仍未创建新的保留集或进入L3。

逐题引用复查更正：BA并未达到16/16，三道成本应用自己的引用缺少排序代价依据，完整语义为13/16。之前把共享解释来源误作题目来源属于AI审阅疏漏，正式BA评测尚未调用模型，已撤回准备状态。AU/AZ等受影响评分也已同步更正，原输出及评分快照保留，见 [引用审阅更正](../eval/l22-completion/citation-review-errata.json)。后续BB将为程序生成的成本主张加入模型选中的共享方法来源，仍逐题检查实际引用。

BB 已通过368项Python/独立PostgreSQL机制测试，无跳过。已见二十题开发回归20/20类型结构、16/16语义、4/4拒答（63次调用），逐字段检查自身引用后进入正式配对验收。新BB保留任务在源码冻结后编写，BA预备但未运行的题不复用；正式结果尚待完整运行和复核，Phase仍进行中。

BB正式配对已完成并失败：19/20结构、13/16语义、4/4拒答；旧C为19/20、4/16、4/4，共131次真实模型调用。开发16/16未复现。详见 [BB正式报告](../eval/l22-completion/FINAL_BB.md)。保留任务已消费，BC继续修复，Phase未完成。

BC通过391项机制测试，已见开发为19/20结构、15/16语义、4/4拒答（63次调用）。唯一失败为误读排序边界后修订触及原预留预算；没有增加预算或替换失败。BC源码已冻结，新保留任务已编写，正式C/BC配对进行中，Phase尚未完成。

## 最终完成

冻结BC完整正式门槛通过，20/20类型、16/16语义、4/4拒答；391项机制测试无跳过，20份成功产物重启与幂等恢复、浏览器、取消通过。完整历史私有备份后，4个隔离课程及542个Session／Run的相关状态已清零。原定Phase完成，线上C与个人配置不变，候选未发布、L3未启动。范围和限制见 [最终报告](../eval/l22-completion/FINAL_BC.md)。
