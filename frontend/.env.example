# Leave VITE_API_BASE_URL empty during Vite development so /api and /actuator
# are proxied by vite.config.ts to VITE_BACKEND_PROXY_TARGET.
VITE_API_BASE_URL=
VITE_BACKEND_PROXY_TARGET=http://127.0.0.1:8080
