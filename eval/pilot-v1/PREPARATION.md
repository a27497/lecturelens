# pilot-v1 准备记录

日期：2026-09-11。分支：`codex/l1-eval-preparation`，基于 C0–C3 清理提交 `88dddb8`。本轮仅增加素材准备脚本与评测文档，没有更改运行时 Java/Python AI 业务路径。

## 已下载

| 视频 | 长度 | 原片大小 | 分辨率 | 英文字幕条数 | 集合 |
| --- | --- | --- | --- | --- | --- |
| Python 入门，第 1 讲 | 43:06 | 96.45 MiB | 480×360 | 887 | dev |
| 线性代数，第 1 讲 | 39:49 | 88.13 MiB | 480×360 | 638 | dev |
| 微观经济学，第 1 讲 | 34:14 | 74.89 MiB | 480×270 | 845 | holdout |

合计约 117 分钟、259.47 MiB 原片、2,370 条字幕。均为从官方资源页链接取得的整节视频，无裁剪，时间偏移 0。字幕及本地格式转换另占少量空间。路径为仓库下 `.data/eval/pilot-v1/`；该目录已被 Git 忽略。

## 本轮验证

- 三段视频、三份字幕完成 SHA-256 和文件大小登记；离线重新验证通过。
- 三段视频均有 H.264 视频和 AAC 音频轨道，字幕非空且时间顺序/边界通过检查。
- 对三段完整视频执行 `ffmpeg -hide_banner -loglevel error -xerror -i <video.mp4> -map 0:v:0 -map 0:a:0 -f null -`，全部退出码 0。各素材目录 `decode-check.txt` 保存结果。完整解码检查是本次额外验证，不包含在下载脚本的常规验证中。
- 各视频 15:00 抽帧保存在 `preview-900s.jpg` 并由 AI 目视检查：Python 片段显示可辨认的课件文字；线性代数显示板书与坐标图；经济学该帧为讲师画面，不能据此确认图表题可行性。
- Python 脚本的字幕标签/实体转换、反向时间区间拒绝、哈希不符拒绝、离线缺文件拒绝均做了临时夹具检查；50 题配额及语言配额求和通过。
- `git diff --check` 通过。未重跑 Java/前端测试：本轮没有修改这些运行时代码。

以上不代表人工逐句确认字幕和语音一致，也不代表视觉信息完整。官方压缩版清晰度较低；后续若视觉题不够或公式无法辨认，应在冻结前调整选材/配额。不要用放大后的模糊图像伪造高质量视觉金标准。

## 已准备、尚待执行

已准备来源/许可清单、资产锁文件、可复现下载与验证脚本、按课程划分的 50 题配额、标注模板和证据映射说明。

后续依次完成：

1. 逐段检查内容与时间对齐，形成 50 题草稿并人工复核，冻结题集。权限夹具用例单独执行。
2. 确定并记录实验输入：固定参考字幕的检索隔离实验，或实际视频解析结果的端到端实验。两者分开报告。
3. 建立本地 source_id 与实际 taskId/revision/evidenceId 的映射，运行当前 keyword baseline，记录失败案例。

当前人工复核题数为 **0**，baseline 运行数为 **0**，付费模型调用数为 **0**。全英文音频不能验证中文 ASR；holdout 仅一门课，当前规模用于建立工程基线。
