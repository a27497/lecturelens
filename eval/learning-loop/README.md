# 真实学习闭环与新课程验收（本轮结果已记录）

2026-09-20，用户授权执行下一步。真实闭环完成并修复历史反馈入口；新课程质量 4/6，Q 回归未改善且已恢复 P，未推进自适应后续练习。详见 [结果](RESULTS.md)。顺序为真实集成闭环、新课程质量验收、同 Session 后续学习决策；阶段证据不足时先修复并重新验收，不把未完成的后续能力声明为已实现。

## 冻结基线与隔离

当前反馈基线为 feedback-P，Python 源码 SHA-256 `6e72c1e52e49a3f876012f585d7a36e12af796fd3c4e8ae5a1a3c86a567768d7`。旧反馈验收、失败记录和已消费保留题保持原样。当前整个工作区的新增备份存放于忽略目录 `.data/learning-loop-20260920/`，不覆盖此前用户修改。

实际测试使用新的 MySQL schema、PostgreSQL 测试库、Redis 容器、消息主题和存储桶，Java/Agent/浏览器端口为 8084/8094/5184。沿用已授权隔离百炼 qwen3-max 生成和复核，没有总调用次数上限；每 Run 的调用、工具、token、时限和候选上限保持。线上 C、账号路由与默认关闭的反馈配置不变。

私有启动器记录模型请求、原始响应、Run 身份和开始/完成/失败调用账本，不改变模型请求或冻结源码；公开报告只保留脱敏结果与哈希。

## 阶段一：完整真实链路

通过真实浏览器注册、上传含字幕的公开课程、等待准备和索引、输入目标并生成新练习。随后保存真实合成作答，运行独立反馈，修改并保存新版本，再次反馈。检查刷新与服务重启恢复、参考答案主动展开、课程证据回看、反馈的精确版本绑定、核对记录、幂等冲突、取消和删除/版本边界。浏览器不替换 API，不注入预置练习。

执行/协议正确性与解释、题目、反馈质量分别记录。任何失败试跑都保留；重试成功不抹去首轮失败。

## 阶段二：新课程边界

选择未用于现有开发或评测的公开课程。先保存来源、引用和课程内/课程外任务，再冻结候选后运行。新课程开发数据一经消费，不再称为保留集；修改候选后使用全新的保留任务。审查题目与答案一致、引用支持、实际作答反馈、可靠拒答及运行时复核误接受/误拒绝。AI 来源核查与用户人工审阅必须分列，不能称 AI 审查为人工验收。

## 阶段三：同 Session 后续学习（尚未实现）

学习目标是根据已保存的作答、独立反馈及用户核对意见选择下一步。模型需要在补充解释、针对性练习、停止之间作出可保存的选择；上下文须绑定具体版本和课程证据。继续共享授权、预算、取消、重放、版本栅栏和幂等协议。此阶段不引入长期薄弱点记忆或自动评分；需要新的机制测试与独立真实质量验收。
